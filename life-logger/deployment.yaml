apiVersion: apps/v1
kind: Deployment
metadata:
  name: life-logger
  namespace: life-logger
  labels:
    app: life-logger
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: life-logger
  template:
    metadata:
      labels:
        app: life-logger
    spec:
      imagePullSecrets:
        - name: ghcr-kj800x
      containers:
        - name: life-logger
          image: ghcr.io/kj800x/life-logger-2:commit-ed6bc1f80c5308fcb841c9f0e04b8f2e76befd09
          command: ["life-logger-2-rs"]
          env:
            - name: SERVER_HOST
              value: "0.0.0.0"
            - name: SERVER_PORT
              value: "8080"
            - name: DATABASE_PATH
              value: "/data/db.db"
          ports:
            - containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          nfs:
            server: 10.60.1.15
            path: /data/services/life-logger
